# ------------------------------------------------------------------
# 1. Database Configuration
# ------------------------------------------------------------------
# ดึงมาจาก DATABASE_URL ของคุณ: postgres://postgres:12341234xxx@...
DB_USER=postgres
DB_PASSWORD=XXXXXXXXXXXXX
DB_NAME=shortlinkqr

# ------------------------------------------------------------------
# 2. Backend Configuration
# ------------------------------------------------------------------
BACKEND_PORT=3001
BASE_URL=http://localhost:3001
# ใช้ Secret เดิมของคุณ
SESSION_SECRET=XXXXXXXXXXXXX
COOKIE_DOMAIN=localhost
USE_HTTPS=false

# ------------------------------------------------------------------
# 3. Frontend Configuration (สำหรับ Docker/Nginx)
# ------------------------------------------------------------------
# เปลี่ยนจาก 5173 (Vite Dev) เป็น 8080 (Nginx Prod)
FRONTEND_PORT=8080
FRONTEND_URL=http://localhost:8080

# ------------------------------------------------------------------
# 4. External Services (Copy มาจากไฟล์เดิมของคุณ)
# ------------------------------------------------------------------
# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=xxxxxxx@xxxxxx.com
SMTP_PASS=XXXXXXXXXXXXX
EMAIL_FROM="Shortlink.QR Support <xxxxxxx@gmail.com>"

# Google OAuth
GOOGLE_CLIENT_ID=xxxxxxx-xxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxxxx_xxxxxxx
# หมายเหตุ: ถ้า Login Google แล้ว Error "Redirect URI mismatch" 
# ให้ลองเปลี่ยน Port ตรงนี้เป็น 8080 หรือเช็คใน Google Console ว่าลงทะเบียน Port ไหนไว้
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback