# ------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------
NODE_ENV=development
PORT=3001
# BASE_URL: URL ของ Backend เอง (Local: http://localhost:3001, Render: https://xxx.onrender.com)
BASE_URL=http://localhost:3001
# FRONTEND_URL: URL ของ Frontend ที่จะยอมให้ CORS ผ่าน (Local: http://localhost:5173, Vercel: https://xxx.vercel.app)
# รองรับหลาย Domain คั่นด้วยลูกน้ำ (,)
FRONTEND_URL=http://localhost:5173,http://localhost:8080

# ------------------------------------------------------------------
# Database (PostgreSQL / Supabase)
# ------------------------------------------------------------------
# PostgreSQL: ใช้ Connection (พอร์ต 5432)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/shortlinkqr?schema=public"
# Supabase: ใช้ Connection String Mode "Transaction" (พอร์ต 6543)
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxx.supabase.co:6543/postgres"

# ------------------------------------------------------------------
# Security & Auth
# ------------------------------------------------------------------
SESSION_SECRET="change_this_to_something_very_secret"
#COOKIE_MAX_AGE_MS=604800000 # 7 days (7 * 24 * 60 * 60 * 1000)
COOKIE_MAX_AGE_MS=900000 # 15 minutes (15 * 60 * 1000)
# - Development = false (ใช้ HTTP ธรรมดา, Cookie Secure=false)
# - Production  = true (ต้องมี SSL, Cookie Secure=true)
USE_HTTPS=false 
# COOKIE_DOMAIN:
# - Local/Docker: localhost
# - Production: .onrender.com (หรือ Custom Domain)
COOKIE_DOMAIN=localhost 

# ------------------------------------------------------------------
# External Services
# ------------------------------------------------------------------
# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# SMTP_SECURE: true สำหรับ port 465, false สำหรับ port อื่นๆ
SMTP_SECURE=false
SMTP_USER=user@gmail.com
SMTP_PASS=app_password
EMAIL_FROM="Shortlink Support <no-reply@shortlink.qr>"

# ------------------------------------------------------------------
# Google OAuth (Optional)
# ------------------------------------------------------------------
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_https://www.google.com/search?q=ID.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
GOOGLE_CALLBACK_URL="http://localhost:3001/api/auth/google/callback"

# For Docker
#GOOGLE_CALLBACK_URL="http://localhost:8080/api/auth/google/callback"
