## ------------------------------------------------------------------
## Application General
## ------------------------------------------------------------------
NODE_ENV=development
PORT=3001
BASE_URL=http://localhost:3001
# CORS: ใส่ URL ของ Frontend (รองรับหลายโดเมนคั่นด้วย ,)
CORS_ORIGIN=http://localhost:5173
TIMEZONE=Asia/Bangkok

## ------------------------------------------------------------------
## Database (PostgreSQL)
## Example: postgresql://<user>:<password>@<host>:<port>/<dbname>
## ------------------------------------------------------------------
DATABASE_URL="postgresql://postgres:password@localhost:5432/shortlinkqr?schema=public"

## ------------------------------------------------------------------
## Session & Authentication
## ------------------------------------------------------------------
SESSION_SECRET="somesuperstrongsecretkey_changeme"
#COOKIE_MAX_AGE_MS=604800000 # 7 days (7 * 24 * 60 * 60 * 1000)
COOKIE_MAX_AGE_MS=900000 # 15 minutes (15 * 60 * 1000)

## ------------------------------------------------------------------
## Security & Production Flags (Flexible Config)
## ------------------------------------------------------------------
# USE_HTTPS:
# - Development = false (ใช้ HTTP ธรรมดา, Cookie Secure=false)
# - Production  = true (ต้องมี SSL, Cookie Secure=true)
USE_HTTPS=false 

# COOKIE_DOMAIN:
# - Development = localhost
# - Production  = .yourdomain.com (เพื่อให้ Subdomain แชร์ Session กันได้)
COOKIE_DOMAIN=localhost 

## ------------------------------------------------------------------
## Email Service (SMTP) - For Forgot Password
## ------------------------------------------------------------------
# แนะนำให้ใช้ Mailtrap (https://mailtrap.io) สำหรับ Development
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
# SMTP_SECURE: true สำหรับ port 465, false สำหรับ port อื่นๆ
SMTP_SECURE=false
SMTP_USER=your_mailtrap_user
SMTP_PASS=your_mailtrap_password
EMAIL_FROM="Shortlink Support <noreply@shortlink.qr>"

## ------------------------------------------------------------------
## Frontend URL (สำหรับสร้างลิงก์ Reset Password ในอีเมล)
## ------------------------------------------------------------------
FRONTEND_URL=http://localhost:5173

## ------------------------------------------------------------------
## Google OAuth (Optional)
## ------------------------------------------------------------------
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_https://www.google.com/search?q=ID.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
GOOGLE_CALLBACK_URL="http://localhost:3001/api/auth/google/callback"

# For Docker
#GOOGLE_CALLBACK_URL="http://localhost:8080/api/auth/google/callback"
